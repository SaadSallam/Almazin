/// Family name must match `pubspec.yaml` (`thmanyahsans`).
/// Add the OTF files under `assets/fonts/` before running the app.
abstract final class AppFonts {
  static const String family = 'thmanyahsans';
}
